apiVersion: v1
kind: Service
metadata:
  name: {{ include "genascope-backend.fullname" . }}
  labels:
    {{- include "genascope-backend.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "genascope-backend.selectorLabels" . | nindent 4 }}
